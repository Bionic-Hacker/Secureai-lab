fastapi==0.136.1
starlette>=1.3.1
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
argon2-cffi==23.1.0
pyjwt==2.13.0
pyotp==2.9.0
cryptography==50.0.1
redis==5.0.8
prometheus-fastapi-instrumentator>=8.0.0
python-magic==0.4.27
boto3==1.35.24
python-multipart==0.0.32
email-validator==2.2.0
httpx==0.27.2
chromadb==0.5.5
clamd==1.0.2
openai==1.51.0
tiktoken==0.8.0
sentence-transformers==5.2.1
transformers>=5.5.0
pypdf==6.15.0
python-docx==1.1.2
bandit==1.7.10
semgrep==1.87.0
# Pinned below 81, not below the CVE fix version (83.0.0) - deliberate,
# tested trade-off, not an oversight. Semgrep own opentelemetry
# dependency still hard-crashes on setuptools>=81 (pkg_resources removal)
# as of this test - confirmed by actually upgrading and re-testing, not
# assumed. The resulting CVE exposure (PYSEC-2026-3447 / CVE-2026-59890)
# is accepted: it describes a macOS-only (APFS/HFS+) Unicode normalization
# bug in MANIFEST.in exclusion handling when BUILDING/PUBLISHING a source
# distribution. This project runs exclusively on Linux containers and
# never builds or publishes its own sdist - setuptools here is used
# purely as a build backend for installing other packages. The
# vulnerability's entire attack surface does not exist in this deployment.
setuptools<81
# Explicit pin - without this, a pre-existing chromadb/semgrep opentelemetry
# version tension sends the resolver backtracking into an old numpy
# release with no prebuilt wheel, which then fails to build from source.
# 1.26.4 is the version that has resolved cleanly in every successful
# build so far.
numpy==1.26.4

# protobuf CVE-2025-4565 (HIGH, DoS via recursion limit bypass in the
# pure-Python backend) - deliberately UNPINNED, not overlooked. Fixed
# in 5.29.6 or 6.33.5, per pip-audit. Three separate attempts to pin it
# here all failed identically with pip resolution-too-deep:
#   protobuf==6.31.1        (exact pin, 6.x line)
#   protobuf==5.29.6        (exact pin, 5.x line)
#   protobuf>=5.29.6,<6.0.0 (range, 5.x line)
# All three converge on the same conclusion: this is not a version-
# choice problem. protobuf sits at the intersection of chromadbs and
# semgreps (via opentelemetry) already-entangled dependency chains, and
# any explicit constraint reopens that tension past pips resolver depth
# limit. The real fix likely requires resolving the chromadb/
# opentelemetry version tension directly - plausibly as a side effect
# of the chromadb upgrade already deferred separately for
# CVE-2026-45830/45831/45833 (see vector_store.py) - not a standalone
# requirements.txt fix.
