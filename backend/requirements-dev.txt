# Test/dev dependencies. None of these were in requirements.txt, which is why
# `pytest` fails immediately: pytest.ini sets `asyncio_mode = auto` (needs
# pytest-asyncio) and `env_files = .env.test` (needs pytest-dotenv), and
# tests/conftest.py imports `from dotenv import load_dotenv` (python-dotenv).
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-dotenv==0.5.2
python-dotenv==1.0.1
